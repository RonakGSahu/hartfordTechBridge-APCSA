
/**
 * Write a description of class CountRainyDays here.
 *
 *Problem 2 - Longest Rainy Streak in Hartford

    You are given a rainfall record for Hartford in an array.
    Each element is:
    1: it rained that day
    0: it did not rain
    
    Your job:
    Find the longest streak of rainy days (longest number of 1s in a row).
 * @author (Md Amit Hasan)
 * @version (Dec 5th 2025)
 */
class CountRainyDays {

    /*
     Input: an array representing rainfall in Hartford (1 = rain, 0 = no rain)
     Returns: the longest rainy streak (longest number of 1s in a row)
    */
    public static int longestRainStreak(int[] rain) {

        // TODO: make an integer variable called currentStreak to count ongoing rainy days

        // TODO: make an integer variable called maxStreak to remember the longest streak found

        // TODO: loop through the rain array using a for-each or for loop

            // TODO: if the current value is 1, increase currentStreak by 1

            // TODO: update maxStreak if currentStreak is now larger

            // TODO: if the current value is 0, reset currentStreak to 0

        // TODO: after the loop ends, return maxStreak

        return 0; // temporary so the code compiles
    }
}
